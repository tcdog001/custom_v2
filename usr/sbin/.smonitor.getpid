#!/bin/bash

. ${__ROOTFS__}/etc/utils/smonitor.in

main() {
	local argc=$#
	local name="$1"
	local count="$2"; count=${count:=3}

	if ((argc<1)); then
		echo 0
	else
		get_smonitor_pid "${name}" "${count}"
	fi
}

main "$@"
